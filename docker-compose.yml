services:
  cloud-media-diagnosis-platform:
    build: .
    container_name: cloud-media-diagnosis-platform
    ports:
      - "8080:8080"
    environment:
      TZ: Asia/Shanghai
    restart: unless-stopped
